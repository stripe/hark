# `hark` actions

These are github actions that the SDKs depend on via their own `changelog.yml` files (plus some internal utilities).

All the externally relevant actions are combined into a `stripe/hark/actions/checks@master` composite for convenience.
